const INITIAL = 0;
const LOADING = 1;
const TRADING = 2;
const PAUSED = 3;
const RESTARTING = 4;
const CRASHED = 5;

export enum AppStatus {
  INITIAL,
  LOADING,
  TRADING,
  PAUSED,
  RESTARTING,
  CRASHED,
}
